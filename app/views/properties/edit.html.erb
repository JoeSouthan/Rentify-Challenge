<%= render 'sidebar' %>

<div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
  <h1><%= t("views.property.edit.edit_property") %></h1>

  <%= render 'form' %>

   <%= link_to 'Show', @property, {class: "btn btn-primary btn-lg"} %> <%= link_to 'Back', properties_path, {class: "btn btn-primary btn-lg"}%>
</div>
